data_science:
  loss_func: CrossEntropyLoss
  n_qubits: ${n_qubits}
  epochs: 2
  learning_rate: 0.03
  optimizer_list: [SGD]
  TEST_SIZE: ${TEST_SIZE}
  TRAINING_SIZE: ${TRAINING_SIZE}
  number_classes: ${number_classes}
  
